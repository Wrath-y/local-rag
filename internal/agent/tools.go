package agent

// ToolRegistry holds tool definitions for the agent (to be expanded later).
type ToolRegistry struct{}

// NewToolRegistry creates an empty ToolRegistry.
func NewToolRegistry() *ToolRegistry {
	return &ToolRegistry{}
}
